{
  "name": "dashahan",
  "version": "0.1.6",
  "private": true,
  "description": "大傻憨: double-click macOS wrapper for deepseek-harness (dsh web)",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build",
    "dev": "tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
